import React from 'react';

const SectionTitle = ({title}) => {
    return (
        <div>
            <h2 className='text-primary font-semibold text-2xl'>{title}</h2>
        </div>
    );
};

export default SectionTitle;